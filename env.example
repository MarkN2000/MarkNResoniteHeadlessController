# Environment Configuration
# 環境変数設定ファイル
#
# ⚠️ セキュリティ警告:
# このリポジトリは公開されているため、.envファイルは.gitignoreで除外されています。
# 本番環境では必ず以下の値を変更してください:
# - AUTH_SHARED_SECRET: 強力なランダム文字列に変更（推奨: 32文字以上）
# - NODE_ENV: production に設定
#
# 環境変数は設定ファイルより優先されます。本番環境では環境変数の使用を強く推奨します。

# Node.js環境（development / production）
# 本番環境では必ず "production" に設定してください
NODE_ENV=development

# サーバーポート番号
SERVER_PORT=8080

# JWT認証シークレット
# ⚠️ 本番環境では必ず強力なランダム文字列に変更してください（推奨: 32文字以上）
# 生成方法例: openssl rand -base64 32 または Node.js: require('crypto').randomBytes(32).toString('base64')
AUTH_SHARED_SECRET=your-secret-key-change-in-production

# Mod API用のAPIキー（現在は使用されていません）
MOD_API_KEY=mod-secret-key

# Resonite Headless Serverの実行ファイルパス
# 例: RESONITE_HEADLESS_PATH=C:/Program Files/Resonite/Headless.exe
# 本番環境では別途設定する仕組みを使用するため、ここでの設定は不要です
RESONITE_HEADLESS_PATH=

# Resonite設定ファイルのディレクトリパス（オプション）
# 未設定の場合は ./config/headless/ を使用
# RESONITE_CONFIG_DIR=./config/headless


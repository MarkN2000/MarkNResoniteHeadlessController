# --- Go (backend) ---
/mrhc
/mrhc.exe
/bin/
/dist/
*.exe
*.test
*.out
# go mod vendor の生成物（race テスト同梱等の一時用途のみ・存在すると
# ローカルビルドが暗黙に -mod=vendor へ切り替わるためコミットしない）
/vendor/

# --- Frontend (React / Vite / Mantine) ---
node_modules/
web/dist/
web/.vite/

# --- ランタイム生成物（セットアップ時に作成・コミットしない） ---
mrhc.config.json
/data/
*.log
# 実機採取fixtureはパーサテストの土台なので明示的に許可
!scripts/empirical-capture/fixtures/**

# Phase 6 e2e 検証の生成物は ephemeral
scripts/e2e-verify/results/

# --- OS / エディタ ---
.DS_Store
Thumbs.db
.idea/
.vscode/

# --- Git Bash / Cygwin crash dumps ---
*.stackdump

# --- Claude ---
.claude/

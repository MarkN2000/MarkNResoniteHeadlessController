{
  "name": "marknresoniteheadlesscontroller",
  "version": "1.0.0",
  "private": true,
  "description": "ResoniteヘッドレスサーバーをブラウザUIから管理するためのツール",
  "type": "module",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "lint": "npm run lint --workspace=backend && npm run lint --workspace=frontend",
    "format": "npm run format --workspace=backend && npm run format --workspace=frontend"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "dotenv": "^16.4.5",
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "typescript": "^5.6.3"
  }
}
